scalar Any

enum Comparison {
  LESS_THAN
  LESS_THAN_OR_EQUAL
  EQUAL_TO
  NOT_EQUAL_TO
  GREATER_THAN
  GREATER_THAN_OR_EQUAL
  CONTAINS
  DOES_NOT_CONTAIN
}

input WhenCondition {
  field: String!
  is: Comparison!
  operand: Any!
}

enum DefinitionFieldTypes {
  array
  boolean
  integer
  number
  string
}

enum StringFormat {
  UUID
  IPv4
  IPv6
  EMAIL
  URI
  ISO_8601
  BASE64
  JSON
  YAML
}

enum StringCasing {
  SNAKE
  CAMEL
  TITLE
  SLUG
  LOWER
  UPPER
}